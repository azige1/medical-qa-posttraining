# Third-Party Code

这个目录用于存放项目依赖的外部框架代码。

当前包含：

- `MedicalGPT/`: 从当前工作区复制的一份 MedicalGPT 框架源码，用于项目开发和训练实验

建议约定：

- 直接基于 `third_party/MedicalGPT/` 中的脚本做项目实验时，要在 `experiments/` 中记录清楚：
  - 使用了哪一个脚本
  - 改了哪些参数
  - 是否修改了源码
- 如果后续对框架源码做了项目级改造，优先把改动记录到项目仓库的实验卡和文档中
